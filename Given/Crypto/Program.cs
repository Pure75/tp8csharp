using System;
using System.Data;
using System.Drawing;

namespace Crypto
{
    class Program
    {
        static void Main(string[] args)
        {
            /*foreach (var VARIABLE in Transposition.Permutation_rule("Authority"))
            {
                Console.Write(VARIABLE); Console.Write(" ");
            }*/

            /*char[,] test = Transposition.Create_table_decrypt("CTARMEYOGTRILISNPNOH", "GOAL", 5);
            for (int i = 0; i < 5; i++)
            {
                for (int j = 0; j < 4; j++)
                {
                    Console.Write(test[i,j] + " ");
                }
                Console.WriteLine();*/
        }
    }
}